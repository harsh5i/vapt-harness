# Reusable Harness Corpus

This folder is reserved for non-target fixtures and generic tool-gap records.

Bug-bounty candidate corpus data lives under:

```text
vapt/bug_bounties/_shared/corpus/
```

The harness reads that shared BB corpus when running corpus and score-tuning
commands.
